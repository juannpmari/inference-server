# ===========================================
# Secrets only — all other configuration lives
# in server_config.yaml
# ===========================================

# HuggingFace
HF_TOKEN=hf_your_token_here

# Unified config path (optional — defaults to server_config.yaml)
# INFERENCE_SERVER_CONFIG=/path/to/inference-server.yaml
